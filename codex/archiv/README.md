# Vivicast Codex Archive

This directory contains completed historical Codex working files.

Do not use files under `codex/archiv/` as active plans, templates, requirements, implementation sources, or status memory.

Only inspect archived files when the Owner explicitly asks for them. Even then, treat every archived file as read-only reference material.

Current archived snapshot:

- `2026-06-28-app-implementation-plan-complete/` - completed app implementation plans and evidence from the finished `APP-IMPLEMENTATION-PLAN.md` cycle.
