<?xml version="1.0" encoding="utf-8"?>
<!--
    Intentionally empty. Every user-facing string lives in :core:designsystem
    (values/strings.xml + values-en/strings.xml). This module previously kept a full
    duplicate copy that shadowed the designsystem values at resource-merge time
    (application resources override library resources), silently defeating string edits
    made in designsystem. Add a string here only if it is genuinely app-module-only.
-->
<resources>
</resources>
